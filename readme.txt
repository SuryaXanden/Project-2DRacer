"Prerequisites"

1) OpenGL must be installed.

sudo apt install freeglut3

2) Sox must be installed.

sudo apt install sox
sudo apt install sox libsox-fmt-all

3) "2dRacerBGM.mp3" and "2dRacer.out" must be in the same directory.

"How to run"

Enter "./2dRacer.out" in the terminal.

Enjoy.
